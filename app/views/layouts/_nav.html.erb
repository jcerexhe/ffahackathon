<ul id="dropdown1" class="dropdown-content">
  <% if user_signed_in? %>
    <% if current_user.admin? %>
      <li><a href="/admin">Admin Dashboard</a></li>
    <% end %>
  <% end %>
  <li><a href="/account"><i class="material-icons left">contacts</i>Stats</a></li>
  <li class="divider"></li>
  <li><%= link_to '<i class="material-icons left">lock_outline</i> Log out'.html_safe, destroy_user_session_path, method: :delete %></li>
</ul>
<nav>
  <div class="nav-wrapper container">
    <a href="/" class="brand-logo">FFA</a>
    <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
    <ul class="right hide-on-med-and-down">
      <%= render 'layouts/nav_headings' %>
      <% if user_signed_in? %>
        <li><a class="dropdown-button" href="#!" data-activates="dropdown1">Account<i class="material-icons right">arrow_drop_down</i></a></li>
      <% else %>
        <li><%= link_to '<i class="material-icons left">lock_open</i> Log in'.html_safe, new_user_session_path %></li>
      <% end %>
    </ul>
    <ul class="side-nav" id="mobile-demo">
      <%= render 'layouts/nav_headings' %>
    </ul>
  </div>
</nav>
